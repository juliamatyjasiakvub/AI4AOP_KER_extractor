streamlit>=1.35.0
pandas>=2.0.0
requests>=2.31.0
pdfplumber>=0.10.0
networkx>=3.0
matplotlib>=3.7.0

# Tests. `pytest` from the repository root runs the suite in tests/, which
# includes the guards on merge classification — the ones that stop opposite
# Key Events ("restored" vs "disrupted") being offered as equivalent.
pytest>=8.0.0
